#!/bin/bash
# Close all open windows (except the Desktop environment and terminals) gracefully

WIN_IDs=$(wmctrl -lx | grep -vwE "Desktop$|xfce4-panel$" | grep -viE "terminal|xterm|alacritty|kitty" | cut -f1 -d' ')
for i in $WIN_IDs
do
  wmctrl -ic "$i"
done
